import { StyleSheet } from 'react-native';
import { wpx, hpx, nf, fonts, colors, wp, hp } from '../../constants/constant';

export default HorizontalStyles = StyleSheet.create({
  horizontalLine: {
    width: wp(90),
    height: wpx(1),
    backgroundColor: '#0000001A',
  },
  smallHorizontalLine: {
    width: wp(20),
    height: hpx(1),
    backgroundColor: '#181818',
  },
});
