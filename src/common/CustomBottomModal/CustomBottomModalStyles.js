import { StyleSheet } from 'react-native';
import { wpx, hpx, nf, fonts } from '../../constants/constant';

const CustomBottomModalStyles = StyleSheet.create({
    mainView: {
        backgroundColor: "red", height: 100, width: 100
    },
    modalView: { backgroundColor: "red", height: 100, width: 100 }
});

export default CustomBottomModalStyles