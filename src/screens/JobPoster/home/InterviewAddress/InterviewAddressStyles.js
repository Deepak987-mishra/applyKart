import {StyleSheet} from 'react-native';
import {colors, fonts, hpx, nf, wpx} from '../../../../constants/constant';

export const InterviewAddressStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.white,
  },
});
