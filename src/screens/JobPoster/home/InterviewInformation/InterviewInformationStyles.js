import {StyleSheet} from 'react-native';
import {colors, fonts, hpx, nf, wpx} from '../../../../constants/constant';

export const InterviewInformationStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.white,
  },
});
