import { StyleSheet } from 'react-native'
import { fonts, hp, nf, wp, wpx } from '../../../constants/constant';

export const JobSeekerDetailStyle = StyleSheet.create({
    footer:{ padding: 10,
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',},
        loadMoreBtn:{ padding: 10,
            backgroundColor: 'gray',
            borderRadius: 4,
            flexDirection: 'row',
            justifyContent: 'center',
            alignItems: 'center',},
            btnText:{textAlign:'center', color:'white'}

})