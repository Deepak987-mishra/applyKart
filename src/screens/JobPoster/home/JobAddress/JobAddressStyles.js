import { StyleSheet } from "react-native";
import { colors, fonts, hpx, nf, wpx } from "../../../../constants/constant";

export const JobAddressStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.white,
  },
});
